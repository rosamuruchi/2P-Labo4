import { ColorCeldaPipe } from './color-celda.pipe';

describe('ColorCeldaPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorCeldaPipe();
    expect(pipe).toBeTruthy();
  });
});
